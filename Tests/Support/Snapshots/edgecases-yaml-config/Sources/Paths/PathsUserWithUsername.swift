// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import NaiveDate
import Get
import HTTPHeaders
import URLQueryEncoder

extension Paths.User {
    public func username(_ username: String) -> WithUsername {
        WithUsername(path: "\(path)/\(username)")
    }

    public struct WithUsername {
        /// Path: `/user/{username}`
        public let path: String

        /// Get user by user name
        public var get: Request<edgecases_yaml_config.User> {
            get throws(GetError) {
                Request(path: path, method: "GET", id: "getUserByName")
            }
        }

        public enum GetError: Error {
            case badRequest
            case notFound
        }

        /// Updated user
        ///
        /// This can only be done by the logged in user.
        public func put(_ body: edgecases_yaml_config.User) throws(PutError) -> Request<Void> {
            Request(path: path, method: "PUT", body: body, id: "updateUser")
        }

        public enum PutError: Error {
            case badRequest
            case notFound
        }

        /// Delete user
        ///
        /// This can only be done by the logged in user.
        public var delete: Request<Void> {
            get throws(DeleteError) {
                Request(path: path, method: "DELETE", id: "deleteUser")
            }
        }

        public enum DeleteError: Error {
            case badRequest
            case notFound
        }
    }
}
