// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import NaiveDate
import HTTPHeaders
import TypedAPI
import URLQueryEncoder

extension Paths {
    public static var pet: Pet {
        Pet(path: "/pet")
    }

    public struct Pet {
        /// Path: `/pet`
        public let path: String

        /// Add a new pet to the store
        public func post(_ body: edgecases_yaml_config.Pet) -> Request<Void, PostError> {
            Request(path: path, method: "POST", body: body, id: "addPet")
        }

        public enum PostError: RequestError {
            case methodNotAllowed
            case unhandled(any Swift.Error)

            public static func decode(statusCode: Int, data: Data, decoder: JSONDecoder) throws -> Self {
                switch statusCode {
                case 405: return .methodNotAllowed
                default: return .unhandled(APIError.unacceptableStatusCode(statusCode))
                }
            }
        }

        /// Update an existing pet
        public func put(_ body: edgecases_yaml_config.Pet) -> Request<Void, PutError> {
            Request(path: path, method: "PUT", body: body, id: "updatePet")
        }

        public enum PutError: RequestError {
            case badRequest
            case notFound
            case methodNotAllowed
            case unhandled(any Swift.Error)

            public static func decode(statusCode: Int, data: Data, decoder: JSONDecoder) throws -> Self {
                switch statusCode {
                case 400: return .badRequest
                case 404: return .notFound
                case 405: return .methodNotAllowed
                default: return .unhandled(APIError.unacceptableStatusCode(statusCode))
                }
            }
        }
    }
}
