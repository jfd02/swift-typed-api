// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import NaiveDate
import HTTPHeaders
import TypedAPI
import URLQueryEncoder

extension Paths.User {
    var login: Login {
        Login(path: path + "/login")
    }

    struct Login {
        /// Path: `/user/login`
        let path: String

        /// Logs user into the system
        func get(username: String, password: String) -> Request<String, GetError> {
            Request(path: path, method: "GET", query: [("username", username), ("password", password)], id: "loginUser")
        }

        enum GetError: RequestError {
            case badRequest
            case unhandled(any Swift.Error)

            static func decode(statusCode: Int, data: Data, decoder: JSONDecoder) throws -> Self {
                switch statusCode {
                case 400: return .badRequest
                default: return .unhandled(APIError.unacceptableStatusCode(statusCode))
                }
            }

            var underlyingError: (any Swift.Error)? {
                switch self {
                case .unhandled(let error): return error
                default: return nil
                }
            }
        }

        enum GetResponseHeaders {
            /// Calls per hour allowed by the user
            @available(*, deprecated, message: "Deprecated")
            static let rateLimit = HTTPHeader<Int32>(field: "X-Rate-Limit")
            /// Date in UTC when toekn expires
            static let expiresAfter = HTTPHeader<Date>(field: "X-Expires-After")
        }
    }
}
