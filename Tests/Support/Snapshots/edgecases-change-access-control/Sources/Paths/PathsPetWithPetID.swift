// Generated by Create API
// https://github.com/jfd02/swift-typed-api

import Foundation
import NaiveDate
import HTTPHeaders
import TypedAPI
import URLQueryEncoder

extension Paths.Pet {
    func petID(_ petID: Int64) -> WithPetID {
        WithPetID(path: "\(path)/\(petID)")
    }

    struct WithPetID {
        /// Path: `/pet/{petId}`
        let path: String

        /// Find pet by ID
        ///
        /// Returns a single pet
        var get: Request<edgecases_change_access_control.Pet, GetError> {
            Request(path: path, method: "GET", id: "getPetById")
        }

        enum GetError: RequestError {
            case badRequest
            case notFound
            case unhandled(any Swift.Error)

            static func decode(statusCode: Int, data: Data, decoder: JSONDecoder) throws -> Self {
                switch statusCode {
                case 400: return .badRequest
                case 404: return .notFound
                default: return .unhandled(APIError.unacceptableStatusCode(statusCode))
                }
            }

            var underlyingError: (any Swift.Error)? {
                switch self {
                case .unhandled(let error): return error
                default: return nil
                }
            }
        }

        /// Updates a pet in the store with form data
        func post(_ body: PostRequest? = nil) -> Request<Void, PostError> {
            Request(path: path, method: "POST", body: body.map(URLQueryEncoder.encode)?.percentEncodedQuery, id: "updatePetWithForm")
        }

        enum PostError: RequestError {
            case methodNotAllowed
            case unhandled(any Swift.Error)

            static func decode(statusCode: Int, data: Data, decoder: JSONDecoder) throws -> Self {
                switch statusCode {
                case 405: return .methodNotAllowed
                default: return .unhandled(APIError.unacceptableStatusCode(statusCode))
                }
            }

            var underlyingError: (any Swift.Error)? {
                switch self {
                case .unhandled(let error): return error
                default: return nil
                }
            }
        }

        struct PostRequest: Encodable, Sendable {
            /// Updated name of the pet
            var name: String?
            /// Updated status of the pet
            var status: String?

            init(name: String? = nil, status: String? = nil) {
                self.name = name
                self.status = status
            }

            var asQuery: [(String, String?)] {
                let encoder = URLQueryEncoder()
                encoder.encode(name, forKey: "name")
                encoder.encode(status, forKey: "status")
                return encoder.items
            }
        }

        /// Deletes a pet
        var delete: Request<Void, DeleteError> {
            Request(path: path, method: "DELETE", id: "deletePet")
        }

        enum DeleteError: RequestError {
            case badRequest
            case unhandled(any Swift.Error)

            static func decode(statusCode: Int, data: Data, decoder: JSONDecoder) throws -> Self {
                switch statusCode {
                case 400: return .badRequest
                default: return .unhandled(APIError.unacceptableStatusCode(statusCode))
                }
            }

            var underlyingError: (any Swift.Error)? {
                switch self {
                case .unhandled(let error): return error
                default: return nil
                }
            }
        }
    }
}
